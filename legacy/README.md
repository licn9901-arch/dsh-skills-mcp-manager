# Legacy dynamic-plugin source

These two files were the original runtime-extension (cordis_define + cordis_run)
source this package was converted from. They are superseded by src/ and are kept
for reference only. The formal package does not use them.
